# Cloudflare Pages configuration
# This file is optional but helps with local development and deployment

name = "craft-pub"
compatibility_date = "2024-01-01"

# Pages configuration
# When using static export (output: 'export'), Next.js outputs to 'out' directory
pages_build_output_dir = "out"

